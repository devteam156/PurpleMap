# NoCompression.java

## Přehled

`NoCompression` je implementace `Compression`, která neprovádí žádnou kompresi. Pouze obaluje streamy do bufferovaných
variant.

## Umístění

`de.bluecolored.bluemap.core.storage.compression.NoCompression`
